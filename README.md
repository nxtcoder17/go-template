## go-template is a cli tool to render a go template file.

It is inspired by need of a helm template like command for quick testing and applying raw YAML manifests, which are not part of a helm chart yet
